<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppRequisitos.Pages.DeviceAuthPage"
             Title="DeviceAuthPage">
    <StackLayout Padding="30" Spacing="10">
        <Label Text="Este dispositivo no está autorizado." 
               FontSize="Large" 
               TextColor="Red" 
               HorizontalOptions="Center"/>
        <Label Text="Android ID:"
               FontSize="24"
               FontAttributes="Bold"
               HorizontalOptions="Center"/>
        <Label Text="{Binding AndroidId}"
       FontSize="40" 
       FontAttributes="Bold"
       TextColor="Black" 
       BackgroundColor="LightGray" 
       Padding="5" 
       HorizontalOptions="Center" />
        <Button Text="Copiar ID" Command="{Binding CopyCommand}" />
        <Button Text="Volver" Command="{Binding VolverCommand}" />
    </StackLayout>
</ContentPage>


